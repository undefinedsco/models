name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: yarn install --non-interactive --ignore-engines

      - name: Build
        run: yarn build

      - name: Test
        run: yarn test:ci

      - name: Check skills
        run: yarn skills:check

      - name: Pack skills
        run: yarn skills:pack
